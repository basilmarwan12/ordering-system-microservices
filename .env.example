# Shared JWT signing secret. Use a long random value outside local development.
JWT_SECRET=replace-with-a-long-random-secret
API_BASE_URL=http://localhost:8080

# Optional database overrides for Docker Compose
MYSQL_ROOT_PASSWORD=root
DB_PASSWORD=root
